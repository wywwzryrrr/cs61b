/*
import org.junit.Assert;
import org.junit.Test;

public class TestSort {
    @Test
    public void testFindSmallest1() {
        String[] input = {"are", "an", "i", "anyway"};
        int expected = 1;
        int actual = Draft.Sort.findSmallest(input, 0);
        Assert.assertEquals(expected, actual);
    }

    @Test
    public void testFindSmallest2() {
        String[] input = {"after", "before", "is", "fuck"};
        int expected = 3;
        int actual = Draft.Sort.findSmallest(input, 2);
        Assert.assertEquals(expected, actual);
    }

    @Test
    public void testSwap() {
        int a = 0;
        int b = 1;
        String[] input = {"a", "b"};
        String[] expected = {"b", "a"};
        Draft.Sort.swap(input, a, b);
        Assert.assertArrayEquals(expected, input);
    }

    @Test
    public void testSort1() {
        String[] input = {"are", "an", "i", "anyway"};
        Draft.Sort.sort(input);
        String[] expected = {"an", "anyway", "are", "i"};
        Assert.assertArrayEquals(expected, input);
    }

    @Test
    public void testSort2() {
        String[] input = {"fuck", "you", "asshole"};
        Draft.Sort.sort(input);
        String[] expected = {"asshole", "fuck", "you"};
        Assert.assertArrayEquals(expected, input);
    }
}*/
